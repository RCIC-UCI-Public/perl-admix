!include common.yaml
---
- package: Sub::Install Perl module 
  name: Sub-Install
  version: "{{versions.Sub_Install}}"
  loc: "{{versions.Sub_Install_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    Sub::Install perl module. install subroutines into packages easily
