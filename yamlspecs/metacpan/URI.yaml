!include common.yaml
---
- package: URI::file Perl module 
  name: URI
  version: "{{versions.URI}}"
  loc: "{{versions.URI_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    URI::file perl module. Uniform Resource Identifiers (absolute and relative)
