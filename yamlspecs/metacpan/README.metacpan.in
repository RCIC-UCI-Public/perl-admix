The perl_@VERSION@-baseline-metacpan RPM installs basic Perl modules needed
for enabling MetaCPAN::Client. Together, they provide basic Perl funcitonality.
The list below shows modules with their versions that are are installed via
their own RPMS. The rpm naming schema is perl_@VERSION@-MODNAME-MODVERSION.

