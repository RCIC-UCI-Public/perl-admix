!include common.yaml
---
- package: Sort-Naturally Perl module 
  name: Sort-Naturally
  version: "{{versions.Sort_Naturally}}"
  loc: "{{versions.Sort_Naturally_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    Sort-Naturally perl module. sort lexically, but sort numeral parts numerically
