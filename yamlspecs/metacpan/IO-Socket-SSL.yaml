!include common.yaml
---
- package: IO::Socket::SSL Perl module 
  name: IO-Socket-SSL
  version: "{{versions.IO_Socket_SSL}}"
  loc: "{{versions.IO_Socket_SSL_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    IO::Socket::SSL perl module. Nearly transparent SSL encapsulation for IO::Socket::INET.
