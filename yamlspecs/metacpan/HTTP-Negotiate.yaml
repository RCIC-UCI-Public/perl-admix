!include common.yaml
---
- package: HTTP::Negotiate Perl module 
  name: HTTP-Negotiate
  version: "{{versions.HTTP_Negotiate}}"
  loc: "{{versions.HTTP_Negotiate_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    HTTP::Negotiate perl module. choose a variant to serve
