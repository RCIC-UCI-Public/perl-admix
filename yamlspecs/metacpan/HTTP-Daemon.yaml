!include common.yaml
---
- package: HTTP-Daemon Perl module 
  name: HTTP-Daemon
  version: "{{versions.HTTP_Daemon}}"
  loc: "{{versions.HTTP_Daemon_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    HTTP-Daemon perl module. A simple http server class
