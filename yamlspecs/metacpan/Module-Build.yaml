!include common.yaml
---
- package: Module::Build Perl module 
  name: Module-Build
  version: "{{versions.Module_Build}}"
  loc: "{{versions.Module_Build_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    Module::Build perl module. Build and install Perl modules
