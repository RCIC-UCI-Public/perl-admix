!include common.yaml
---
- package: HTTP::Date Perl module 
  name: HTTP-Date
  version: "{{versions.HTTP_Date}}"
  loc: "{{versions.HTTP_Date_loc}}"
  vendor_source: https://cpan.metacpan.org/authors/id/{{loc}}/{{name}}-{{version}}.{{extension}}
  description: >
    HTTP::Date perl module. HTTP::Date - date conversion routines
